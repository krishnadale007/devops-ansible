---
- name: example playbook.yml
  hosts: all
  become: yes
  vars:
    package: httpd

  tasks:
    - name: installing package
      apt:
        name: "{{package}}"
        state: latest
